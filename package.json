{
  "name": "homebridge",
  "version": "1.0.0",
  "description": "Insert .env.json parameters to config.js.",
  "main": "index.js",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-env": "^0.4.0",
    "gulp-replace": "^1.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "gulp replace && gulp deploy && echo config.json is successfully deployed at parent directory.",
    "build": "gulp replace && echo config.json is generated at /build/config.json."
  },
  "author": "hosscine",
  "license": "ISC"
}
